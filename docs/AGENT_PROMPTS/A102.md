AGENT_ID: A102
RÔLE: Ingénieur Full-stack
MODULE: RDV & Agenda
OBJECTIF: Mettre en place la gestion des rendez-vous (agenda hebdo, création/annulation, disponibilité praticien/salle) avec notifications internes.
CONTEXTE: Stack Node/Express + TypeScript + PostgreSQL pour backend (migrations nécessaires), Next.js 16 + Tailwind pour UI (vue agenda). Orchestrateur jobs Render pour rappels (connecteur existant). Auth JWT-RBAC.
TÂCHES:
  1) Conception: schéma SQL `appointments`, `providers`, `rooms`, `appointment_notes`; règles double-booking, durées, fuseau Europe/Paris. Définir endpoints REST et composants UI (vue colonne par praticien).
  2) Implémentation: endpoints `GET /api/appointments`, `POST /api/appointments`, `PATCH /api/appointments/:id/cancel`, websocket/polling channel pour mises à jour; UI agenda (drag/drop ou sélection créneau); tests unitaires+intégration coverage ≥ 80%.
  3) Documentation: renseigner /docs/rdv.md (agent_id, comment, quand UTC, pourquoi), mettre à jour /docs/openapi.yaml (schemas + paramètres query) et mentionner règles métier. Ajouter exemples d’utilisation dans README si besoin.
  4) Déploiement: variables `APPOINTMENTS_SLOT_MINUTES`, migrations versionnées, seed dev (2 praticiens, 1 semaine de RDV), configuration Render (job pour recalcul dispos si nécessaire).
ACCEPTATION (DoD):
  - Tests locaux verts, lint/CI OK, RBAC appliqué, OpenAPI/docs à jour, aucun secret committé.
SORTIES ATTENDUES:
  - Branche: feature/rdv-agenda-A102
  - Liste des fichiers modifiés + migrations + endpoints
  - Résumé technique (500–800 caractères)
  - Entrée ajoutée à /docs/CHANGELOG.md (format requis)
FORMAT LIVRAISON:
  - Ouvrir une PR; coller lien PR + extrait de CHANGELOG ajouté.
