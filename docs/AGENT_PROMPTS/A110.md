AGENT_ID: A110
RÔLE: Ingénieur Full-stack
MODULE: Paramétrage cabinet
OBJECTIF: Proposer une interface admin pour configurer horaires, salles, TVA, prestataires (SMS/email), et exposer ces réglages via API pour les autres modules.
CONTEXTE: Stack Node/Express + TypeScript + PostgreSQL (table `settings`/JSONB ou tables dédiées), Next.js 16 UI (formulaire multi-sections). Auth RBAC (admin only).
TÂCHES:
  1) Conception: schéma SQL `settings` (clé/valeur versionnée) + tables liées (`business_hours`, `integrations`), validations, stratégie versioning + historisation.
  2) Implémentation: endpoints `GET /api/settings`, `PUT /api/settings`, `GET /api/settings/history`; UI Next (form wizard). Propager caches (emitter) pour modules RDV/facturation; tests coverage ≥ 80%.
  3) Documentation: /docs/parametrage.md (agent_id, comment, quand UTC, pourquoi), mise à jour /docs/openapi.yaml, README instructions sur comment modifier settings via CLI/API.
  4) Déploiement: variables `DEFAULT_TVA_RATE`, migrations SQL initiales, seed dev (horaires 8h-19h, TVA 20%), Render env sync.
ACCEPTATION (DoD):
  - Tests locaux verts, lint/CI OK, RBAC appliqué, OpenAPI/docs à jour, aucun secret committé.
SORTIES ATTENDUES:
  - Branche: feature/parametrage-A110
  - Liste des fichiers modifiés + migrations + endpoints
  - Résumé technique (500–800 caractères)
  - Entrée ajoutée à /docs/CHANGELOG.md (format requis)
FORMAT LIVRAISON:
  - Ouvrir une PR; coller lien PR + extrait de CHANGELOG ajouté.
